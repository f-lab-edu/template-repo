services:
  app:
    build:
      context: .
      dockerfile: app/Dockerfile
    ports:
      - "8080:8080"
    env_file:
      - .env
    environment:
      REDIS_SENTINEL_MASTER: mymaster
      REDIS_SENTINEL_NODES: redis-master:26379,redis-replica1:26379,redis-replica2:26379
    depends_on:
      postgres:
        condition: service_healthy
    networks:
      - appnet
      - proxynet
      - redisnet

  postgres:
    image: postgres:18.0
    container_name: postgres
    env_file:
      - .env
    environment:
      POSTGRES_DB: ${DB_NAME}
      POSTGRES_USER: ${DB_USER}
      POSTGRES_PASSWORD: ${DB_PASSWORD}
      TZ: Asia/Seoul
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql
    networks:
      - appnet
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U ${DB_USER} -d ${DB_NAME}"]
      interval: 10s
      timeout: 5s
      retries: 5

volumes:
  postgres_data:
    driver: local

networks:
  appnet:
    driver: bridge
  proxynet:
    external: true
  redisnet:
    external: true