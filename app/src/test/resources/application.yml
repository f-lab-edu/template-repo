spring:
  datasource:
    url: jdbc:tc:postgresql:18:///app?TC_INITSCRIPT=schema.sql

  jpa:
    hibernate:
      ddl-auto: none
    show-sql: true
    properties:
      hibernate:
        format_sql: true
        dialect: org.hibernate.dialect.PostgreSQLDialect